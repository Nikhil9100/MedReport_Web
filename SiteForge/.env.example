# OpenAI API Configuration
# Required for medical report analysis using GPT-5
OPENAI_API_KEY=your_openai_api_key_here

# Session Configuration
# Optional: Secret key for session encryption (auto-generated if not provided)
SESSION_SECRET=your_session_secret_here

# Database Configuration
# Optional: PostgreSQL connection string for persistent data storage
# If not provided, sessions are stored in-memory (will be lost on restart)
DATABASE_URL=postgresql://user:password@localhost:5432/medrep

# Deployment Configuration
# Node environment - production for Vercel, development for local
NODE_ENV=production

# Port Configuration
# Vercel automatically sets PORT environment variable
# This is only needed for local development
PORT=5000
